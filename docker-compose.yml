services:
  basione-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: basione-server
    restart: always
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}